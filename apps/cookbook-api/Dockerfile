# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build cookbook-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t cookbook-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system cookbook-api && \
          adduser --system -G cookbook-api cookbook-api

COPY dist/apps/cookbook-api cookbook-api
RUN chown -R cookbook-api:cookbook-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix cookbook-api --omit=dev -f install

CMD [ "node", "cookbook-api" ]
